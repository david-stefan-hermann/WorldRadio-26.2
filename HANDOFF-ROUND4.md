# World Radio – round 4: recipes, on/off switch, texture fixes

Read `HANDOFF-PLAN.md` (architecture, tools, status) and `../CLAUDE.md` (Gradle line, reporting rules) first. State:
round 3 done (antennas, `level=0..3`, right-click opens the screen), jar 0.3.0. Six points, all confirmed by the user
on 2026-09-25. Version `0.4.0+26.2`.

Textures are generated by `tools/MakeTextures.java` (run it, then copy/it writes into `src/main/resources`; check its
`main`). `art/blocks/build_blocks.py` is an older draft and stays untouched. Every texture change is done in the
generator, never by hand-editing PNGs, and ends with a regenerated `docs/blocks.png`.

Order: 3 → 4 → 1 → 6 → 2 → 5 (textures last, one screenshot for all three).

## 3. +32 blocks per antenna block

`Antenna.STEP` 8 → 32 (`Config.Values.antennaStep` default follows). Range = 32 + 32 × count, cap stays
`maxAntenna = 32` → 1056 blocks; ask nothing, the user wants "ever further". Update `SignalGraphTest` (expected
32/64/128/1056 for 0/1/3/40), `tools/dev-test-network.sh` (3 iron bars → `range=128`, one broken → `range=96`,
mixed column of four → `range=160`), README (formula, config example, the hint text
`worldradio.range.hint` says "+%s blocks each" – it takes the config value, check it prints 32).

## 4. On/off without clearing the station

- `RadioBlockEntity`: new boolean `enabled` (default true), saved (`putBoolean("enabled")`, `getBooleanOr("enabled",
  true)` so round-3 radios stay on) and synced with the other fields. A radio plays only when `enabled && !url.isEmpty()`:
  `RadioNetwork.recompute()` (skip disabled radios as signal sources, `Antenna.level(enabled && hasUrl, antenna)`),
  `SourceTracker` (client: no candidate when `!radio.enabled()`), `RadioCommand status` prints `enabled=…`.
- Screen: a toggle button next to **Clear** at the bottom of `RadioScreen`, label `worldradio.station.on` /
  `worldradio.station.off` ("Turn off" / "Turn on" – it names the action, not the state; de: "Ausschalten" /
  "Einschalten"). Sends a new C2S packet `Packets.SetEnabled(pos, boolean)` with the same reach check as
  `SetStation`. The status line when disabled: `worldradio.status.disabled` = "Off – press Turn on or sneak +
  right-click the radio" / "Aus – Einschalten drücken oder Schleichen + Rechtsklick". Move `worldradio.volume.hint`
  if the bottom row gets too tight (Clear 60 px, toggle 70 px, hint from x = 140).
- Block: sneak + right-click on the **radio** toggles `enabled` server side (click sound, overlay message
  `worldradio.toggle.on` / `worldradio.toggle.off` "Radio on" / "Radio off"), plain right-click still opens the
  screen. Implement in `RadioBlock` (override `useWithoutItem`/`useItemOn`, call `super` when not sneaking).
  Vanilla skips block use on sneak + use with an item in hand, so this works with an empty hand only – README says
  so (same wording as in round 2). The amplifier has no switch (it just relays).
- Evidence: `dev-test-network.sh` – tune a radio, `/worldradio enable <pos> false` (add the dev sub-command) → the
  amplifier at 20 loses the signal and `status` prints `enabled=false url=<still set>`, `true` → signal back;
  dev-client log line for sneak + right-click toggling; screenshot `docs/favourites.png` shows the new button.

## 1. Proper crafting recipes

Replace the two placeholder recipes (`data/worldradio/recipe/*.json`):

- **Radio** (shaped, category `redstone`):
  ```
  P I P      P = #minecraft:planks, I = iron_ingot
  C N R      C = copper_ingot (the coil), N = note_block (the speaker), R = redstone (the tuner)
  P P P
  ```
  Nine items, one note block: a wooden case with a speaker and a tuning circuit.
- **Radio Amplifier** (shaped, `redstone`):
  ```
  I C I      I = iron_ingot, C = copper_ingot
  C W C      W = worldradio:radio (the amplifier is a radio in a steel case that re-sends)
  I R I      R = redstone
  ```
  Requiring a radio ties the two blocks together and makes the amplifier the more expensive one, which fits its
  reach.
- Add `data/worldradio/advancement/recipes/…` unlock advancements (has copper ingot → both recipes) the way vanilla
  does (`minecraft:recipes/redstone/...` as template from the client jar's data folder), so the recipe book shows
  them. Check the 26.2 folder name (`advancement` singular) in the jar.
- Evidence: dev server `recipe give @s worldradio:radio` works, `/give` a crafting table scene is not needed – a
  dev-client screenshot of the recipe book page is nice but optional; at least the datapack loads without a
  `Couldn't parse` warning in the server log (grep it). README "Crafting" section lists both grids.

## 6. Front texture: speaker one pixel narrower, right part one pixel in

Current radio front (16 px, x from 0): frame x=0/15, inner panel x=1..14; grille x=2..7 (6 wide), divider x=8,
meter/reach x=9..14 – the right side touches the frame at x=15 with no panel pixel between, while top (y=1),
bottom (y=14) and left (x=1) have one. New layout for **both** fronts, all frames and all levels:

- grille (radio) / speaker (amplifier, see point 2): x=2..6 (5 wide), y=2..13
- divider column x=7 (panel colour)
- meter + reach: x=8..13 (shift everything of the right part one pixel left: `METER_X` {10,12,14} → {9,11,13},
  the LED and the reach bars likewise)
- x=14 panel colour, x=15 frame
- The grille ring animation centre moves with the grille (centre of x=2..6, i.e. 4.0).

## 2. Amplifier front gets a speaker like the radio's

Replace `coil()` with the radio's `grille()` (same checker of holes, same ring animation while on) in the
amplifier's palette – keep the amplifier's blue LED/lit colours so the two blocks stay distinguishable by case
colour (steel vs wood) and light colour (blue vs green). Delete `coil()` if nothing else uses it.

## 5. Antenna socket on top of both blocks

`caseTexture(p, vents)` for the top: keep the rim and grain; replace the three vent slits with a socket in the
centre: a 6×6 plate x=5..10, y=5..10 in the palette's `light` tone, a 4×4 dark ring x=6..9 (`0x222222`), a 2×2 hole
x=7..8 in near-black (`0x0B0B0B`). Two short vent slits remain left and right of the plate (x=2..3 and x=12..13 at
y=6 and y=8) so the top still reads as a case. Side textures unchanged. Both blocks use the same drawing with their
own palette.

Evidence for 6, 2, 5: regenerate, build, `tools/dev-screenshot.sh` scene with a radio and an amplifier (front and
top visible: view from above-front, e.g. pitch 35) each at level 0 and level 2 with two iron bars on top of one of
them → `docs/blocks.png`; also print the 16×16 character map of `radio_front_1` frame 0 and `radio_top` with the
python snippet the user's session used (PIL, one letter per colour) and check the x=14 column is panel colour.

## Reporting

Per point: done / evidence / deviations. Jar path `build/libs/worldradio-0.4.0+26.2.jar`; the user tests in Prism
(the switch and the textures are visual/acoustic acceptance). Do not commit. Update `HANDOFF-PLAN.md` status and
README (recipes, +32, switch, gestures table: right-click = screen, sneak + right-click on the radio = on/off).
